{
  "name": "bong-calender",
  "private": true,
  "version": "1.0.0",
  "description": "Bengali Panjika calendar - web and desktop app",
  "workspaces": [
    "packages/*",
    "apps/web",
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run build:core && npm run dev -w apps/web",
    "build": "npm run build -w packages/panjika-core && npm run build -w apps/web",
    "build:core": "npm run build -w packages/panjika-core",
    "typecheck": "npm run typecheck -w packages/panjika-core && npm run typecheck -w apps/web",
    "desktop:dev": "npm run tauri dev -w apps/desktop",
    "desktop:build": "npm run tauri build -w apps/desktop"
  }
}
